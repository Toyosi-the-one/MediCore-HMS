{
  "name": "medicore-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/medicore-app/src",
  "targets": {
    "serve": {
      "command": "ng serve",
      "options": {
        "cwd": "apps/medicore-app"
      }
    },
    "build": {
      "command": "ng build",
      "options": {
        "cwd": "apps/medicore-app"
      }
    }
  }
}